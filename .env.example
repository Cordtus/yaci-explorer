# Yaci Explorer Configuration
# Copy to .env and update the required values

# =============================================================================
# REQUIRED - You must set these
# =============================================================================

# gRPC endpoint of the chain to index
CHAIN_GRPC_ENDPOINT=localhost:9090

# PostgreSQL password (change in production!)
POSTGRES_PASSWORD=changeme

# =============================================================================
# OPTIONAL - Defaults work for most setups
# =============================================================================

# PostgreSQL connection
POSTGRES_USER=postgres
POSTGRES_DB=yaci

# PostgREST API URL for the frontend
# Use /api for reverse proxy setups (recommended)
# Use http://localhost:3000 for direct access during development
VITE_POSTGREST_URL=/api

# Chain REST endpoint for IBC denom resolution (optional)
# VITE_CHAIN_REST_ENDPOINT=http://localhost:1317

# =============================================================================
# ADVANCED - Usually don't need to change
# =============================================================================

# Yaci indexer settings
# YACI_MAX_CONCURRENCY=100
# YACI_INSECURE=true
